# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/
namespace: openshift-cluster-api

resources:
- ../../crd/bases/cluster.x-k8s.io_clusters.yaml
- ../../crd/bases/cluster.x-k8s.io_machines.yaml
- ../../crd/bases/cluster.x-k8s.io_machinesets.yaml
- ../../crd/bases/cluster.x-k8s.io_machinedeployments.yaml
- ../../crd/bases/exp.cluster.x-k8s.io_machinepools.yaml
- ../../crd/bases/addons.cluster.x-k8s.io_clusterresourcesets.yaml
- ../../crd/bases/addons.cluster.x-k8s.io_clusterresourcesetbindings.yaml
- ../../crd/bases/cluster.x-k8s.io_machinehealthchecks.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
- ../../crd/patches/webhook_in_clusters.yaml
- ../../crd/patches/webhook_in_machines.yaml
- ../../crd/patches/webhook_in_machinesets.yaml
- ../../crd/patches/webhook_in_machinedeployments.yaml
- ../../crd/patches/webhook_in_machinehealthchecks.yaml
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable webhook, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
- patches/cainjection_in_machines.yaml
- patches/cainjection_in_machinesets.yaml
- patches/cainjection_in_machinedeployments.yaml
- patches/cainjection_in_machinehealthchecks.yaml
# +kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
